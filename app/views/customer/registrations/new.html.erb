<div class="container">
  <div class="row">
    <div class="col">
      新規会員登録
      お気に入り登録・お問い合わせを行う際は会員登録が必須です
      既に会員の方は<%= link_to 'こちらへ', new_customer_session_path %>
       <%= form_with model: @customer, url: registration_path(resource_name) do |f| %>
        <%= render "customer/shared/error_messages", resource: resource %>

        <div class="field">
          名前<br>
          <%= f.text_field :name %>
        </div>

        <div class="field">
         メールアドレス<br>
         <%= f.email_field :email %>
        </div>

        <div class="field">
         電話番号<br>
         <%= f.text_field :telephone_number %>
        </div>

         <div class="field">
          パスワード
           <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> 文字以上)</em>
           <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password" %>
         </div>

         <div class="field">
          パスワード確認用<br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
         </div>

         <div class="actions">
          <%= f.submit "登録する", class: "btn btn-outline-dark"%>
         </div>
       <% end %>
    </div>
  </div>
</div>